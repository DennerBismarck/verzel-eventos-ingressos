# Copie para .env.local (que NÃO vai pro git).
#
# NEXT_PUBLIC_ é um prefixo especial do Next: essas variáveis são embutidas
# no bundle que vai pro browser. Ou seja: NUNCA coloque segredo aqui.
# A chave do TMDb, por exemplo, mora só no backend.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
